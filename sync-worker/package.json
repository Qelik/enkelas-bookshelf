{
  "name": "enkelas-bookshelf-sync",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --var AUTH_SECRET:\"$(sed -n 's/^AUTH_SECRET=//p' .env)\"",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
